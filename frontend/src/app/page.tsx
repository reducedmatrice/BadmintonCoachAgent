import { MinimalChatDemo } from "@/components/workspace/minimal-chat-demo";

export default function HomePage() {
  return (
    <MinimalChatDemo
      title="Badminton-Coach-Agent"
      subtitle="前端已收敛为最小可运行 demo。现在先保留一个简单对话窗口，后续再逐步接回真实线程流和 agent 能力。"
    />
  );
}
